<h1><%= @student.to_s %></h1>
<div><%= "This student is currently " + (@student.active ? "active." : "inactive.") %></div>
<div><%= link_to "Activate Student", activate_student_path(@student) %></div>